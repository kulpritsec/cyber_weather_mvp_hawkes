# Cyber Weather Forecast — MVP (Hawkes)

Backend: FastAPI + SQLite + NumPy/SciPy; Frontend: Vite + React + Leaflet.

## Steps
1) Generate data → 2) Nowcast → 3) Hawkes fit → 4) Run API → 5) Open UI.

See `backend/README.md` for exact commands.
